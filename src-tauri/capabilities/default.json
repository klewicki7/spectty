{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Baseline permissions for the Spectty main window. M0 only needs core invoke + the event channel that carries the pong proof.",
  "windows": ["main"],
  "permissions": ["core:default", "core:event:default"]
}
